const index = require('.././index.js');

describe('countingOutNum', () => {
    test('', () => {
        expect(index.countingOutNum('')).toBe();
    });

    test('', () => {
        expect(index.countingOutNum('')).toBe();
    });

    test('', () => {
        expect(index.countingOutNum('')).toBe();
    });

    test('', () => {
        expect(index.countingOutNum('')).toBe();
    });

     test('', () => {
        expect(index.countingOutNum('')).toBe();
     });
    
      afterEach(() => {
        jest.clearAllMocks();
    });
})


